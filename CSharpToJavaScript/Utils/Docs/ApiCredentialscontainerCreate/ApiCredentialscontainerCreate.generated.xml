<docs>
<CredentialsContainerCreate>
<summary>
The <strong><c>create()</c></strong> method of the <see cref="CredentialsContainer."/> interface returns a {{jsxref("Promise")}} that resolves with a new credential instance based on the provided options, the information from which can then be stored and later used to authenticate users via <see cref="CredentialsContainer.Get."/>.
</summary>
<remarks>
<para>This is used by multiple different credential-related APIs with significantly different purposes:</para><para>The below reference page starts with a syntax section that explains the general method call structure and parameters that apply to all the different APIs. After that, it is split into separate sections providing parameters, return values, and examples specific to each API.</para><blockquote class="NOTE"><h5>NOTE</h5><para><strong>Note:</strong> This method is restricted to top-level <see href="https://developer.mozilla.org/en-US/docs/Glossary/browsing context">browsing context</see> (i.e., a document running directly inside a browser tab, and not embedded inside another document). Calls to it from within an <c>&amp;lt;iframe&amp;gt;</c> element will resolve without effect.</para></blockquote>
<para><seealso href="https://developer.mozilla.org/en-US/docs/Web/API/CredentialsContainer/create"> <em>See also on MDN</em> </seealso></para>
</remarks>
<returns>A {{jsxref("Promise")}} that resolves with a <see cref="Credential."/> instance matching the provided parameters:If a single credential cannot be successfully created, the Promise will resolve to <c>null</c>.A {{jsxref("Promise")}} that resolves with an <see cref="PublicKeyCredential."/> instance matching the provided parameters. If no credential object can be created, the promise resolves with <c>null</c>.</returns>
</CredentialsContainerCreate>
</docs>