<docs>
<MediaTrackSettingsDeviceId>
<summary>
The <see cref="MediaTrackSettings."/> dictionary's<br/><strong><c>deviceId</c></strong> property is a string which<br/>uniquely identifies the source for the corresponding <see cref="MediaStreamTrack."/> for<br/>the origin corresponding to the browsing session. This lets you determine what value was<br/>selected to comply with your specified constraints for this property&amp;apos;s value as<br/>described in the <see cref="MediaTrackConstraints.DeviceId."/> property you provided<br/>when calling either <see cref="MediaDevices.GetUserMedia."/>.
</summary>
<remarks>
<para>If needed, you can determine whether or not this constraint is supported by checking<br/>the value of <see cref="MediaTrackSupportedConstraints.DeviceId."/> as returned by a<br/>call to <see cref="MediaDevices.GetSupportedConstraints."/>. However, typically this<br/>is unnecessary since browsers will ignore any constraints they&amp;apos;re unfamiliar with.</para><para>Because <see href="https://developer.mozilla.org/en-US/docs/Glossary/RTP">RTP</see> doesn't include this information, tracks associated with a<br/><see href="https://developer.mozilla.org/en-US/docs/Web/API/WebRTC_API">WebRTC</see> <see cref="RTCPeerConnection."/><br/>will never include this property.</para>
<para>-<see href="https://developer.mozilla.org/en-US/docs/Web/API/Media_Capture_and_Streams_API">Media Capture and Streams API</see><br/>-<see href="https://developer.mozilla.org/en-US/docs/Web/API/Media_Capture_and_Streams_API/Constraints">Capabilities, constraints, and settings</see><br/>-<see cref="MediaTrackSettings.GroupId."/><br/>-<see cref="MediaTrackConstraints.DeviceId."/><br/>-<see cref="MediaTrackSettings."/><br/></para>
<para><seealso href="https://developer.mozilla.org/en-US/docs/Web/API/MediaTrackSettings/deviceId"> <em>See also on MDN</em> </seealso></para>
</remarks>
<value>A string whose value is an origin-unique identifier for the track&amp;apos;s<br/>source. This ID is valid across multiple browsing sessions for the same origin and is<br/>guaranteed to be different for all other origins, so you can safely use it to request<br/>the same source be used for multiple sessions, for example.The actual value of the string, however, is determined by the source of the track, and<br/>there is no guarantee what form it will take, although the specification does recommend<br/>it be a GUID.Since there is a one-to-one pairing of ID with each source, all tracks with the same<br/>source will share the same ID for any given origin, so<br/><see cref="MediaStreamTrack.GetCapabilities."/> will always return exactly one value<br/>for <c>deviceId</c>. That makes the device ID not useful for any changes to<br/>constraints when calling <see cref="MediaStreamTrack.ApplyConstraints."/>.<blockquote class="NOTE"><h5>NOTE</h5><strong>Note:</strong> An exception to the rule that device IDs are the same across browsing sessions:<br/>private browsing mode will use a different ID, and will change it each browsing<br/>session.</blockquote></value>
</MediaTrackSettingsDeviceId>
</docs>