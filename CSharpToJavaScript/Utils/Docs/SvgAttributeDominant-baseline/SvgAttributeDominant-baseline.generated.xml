<docs>
<SVGAttributeDominant-baseline>
<summary>
The <strong><c>dominant-baseline</c></strong> attribute specifies the dominant baseline, which is the baseline used to align the box&amp;apos;s text and inline-level contents. It also indicates the default alignment baseline of any boxes participating in baseline alignment in the box&amp;apos;s alignment context.
</summary>
<remarks>
<para>It is used to determine or re-determine a scaled-baseline-table. A scaled-baseline-table is a compound value with three components:</para><para>Some values of the property re-determine all three values. Others only re-establish the baseline-table font-size. When the initial value, <c>auto</c>, would give an undesired result, this property can be used to explicitly set the desired scaled-baseline-table.</para><para>If there is no baseline table in the nominal font, or if the baseline table lacks an entry for the desired baseline, then the browser may use heuristics to determine the position of the desired baseline.</para><blockquote class="NOTE"><h5>NOTE</h5><para><strong>Note:</strong> As a presentation attribute, <c>dominant-baseline</c> can be used as a CSS property.</para></blockquote><para>You can use this attribute with the following SVG elements:</para>
<para><seealso href="https://developer.mozilla.org/en-US/docs/Web/SVG/Attribute/dominant-baseline"> <em>See also on MDN</em> </seealso></para>
</remarks>
</SVGAttributeDominant-baseline>
</docs>