<docs>
<SVGElementCursor>
<summary>
<div class="IMPORTANT"><h5>IMPORTANT</h5> <strong>Deprecated</strong></div> <blockquote class="NOTE"><h5>NOTE</h5><strong>Note:</strong> The CSS <see href="https://developer.mozilla.org/en-US/docs/Web/CSS/cursor">cursor</see> property should be used instead of this element.</blockquote>
</summary>
<remarks>
<para>The <strong><c>&amp;lt;cursor&amp;gt;</c></strong> <see href="https://developer.mozilla.org/en-US/docs/Web/SVG">SVG</see> element can be used to define a platform-independent custom cursor. A recommended approach for defining a platform-independent custom cursor is to create a PNG image and define a <c>cursor</c> element that references the PNG image and identifies the exact position within the image which is the pointer position (i.e., the hot spot).</para><para>The PNG format is recommended because it supports the ability to define a transparency mask via an alpha channel. If a different image format is used, this format should support the definition of a transparency mask (two options: provide an explicit alpha channel or use a particular pixel color to indicate transparency). If the transparency mask can be determined, the mask defines the shape of the cursor; otherwise, the cursor is an opaque rectangle. Typically, the other pixel information (e.g., the R, G and B channels) defines the colors for those parts of the cursor which are not masked out. Note that cursors usually contain at least two colors so that the cursor can be visible over most backgrounds.</para>
<para><seealso href="https://developer.mozilla.org/en-US/docs/Web/SVG/Element/cursor"> <em>See also on MDN</em> </seealso></para>
</remarks>
</SVGElementCursor>
</docs>